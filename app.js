const codes=[];const grid=document.getElementById('codesGrid');if(!codes.length){grid.innerHTML='<article><label>READY FOR VERIFIED CODES</label><h3>No code is marked active yet</h3><p>Only publish codes after checking them. This avoids showing expired or fake codes to visitors.</p></article>'}